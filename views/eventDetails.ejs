<!DOCTYPE html>
<html>
    <head>
        <title>Event Details</title>
        <link rel="stylesheet" href="/css/style.css">
    </head>
    <body>
        <%- include('partials/navbar') %>
        <%- include('partials/flash') %>

        <div class="container">
            <h1> <%= event.title %> </h1>
            <h3> <%= event.description %> </h3>
            <p> <%= event.location %> | <%= event.event_date %></p>

            <% 
            // Compute remaining here so the UI can reflect real-time availability
            // without duplicating this logic across multiple templates.
            const remaining = event.total_capacity - event.tickets_sold; 
            %>

            <p> Tickets Available: <%= remaining %> </p>

            <!-- Disable purchase UI when sold out to reduce frustration; server still enforces it -->
            <% if (remaining <= 0) { %>
                <h3> This event is sold out </h3>
                <button disabled> Sold Out </button>
            <% } else {%>
                <!-- eventId is the source of truth; userId is also tracked in session server-side -->
                <form class="event-form" action="/bookings" method="POST">
                    <input type="hidden" name="eventId" value= "<%= event.event_id %>" >
                    <input type="hidden" name="userId" value= "<%= userId %>">
    
                    <label> How many tickets? </label>
                    <!-- Min/max guides the user; server validates to prevent overbooking -->
                    <input type="number" name="ticketQuantity" min= "1" max="<%= remaining %>">
                    <button type="submit">Submit</button>
                </form>
            <% } %>
        </div>
    </body>
    
</html>